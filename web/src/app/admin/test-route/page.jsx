export default function AdminTestRoutePage() {
  return (
    <main style={{ minHeight: '100vh', padding: 32, background: '#fdf4ff', color: '#581c87' }}>
      <h1>Admin test route works</h1>
      <p>If this page loads in production, the latest React Router route config is deployed.</p>
    </main>
  );
}
